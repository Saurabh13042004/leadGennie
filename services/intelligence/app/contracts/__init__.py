"""Pydantic contract models — the single source of truth (see docs/intelligence-engine/api-contract.md).

FastAPI publishes these as OpenAPI; the Next.js app generates TS types from it and keeps zod mirrors.
"""
